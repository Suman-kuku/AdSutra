interface Props {
    showId: string;
    /** Pre-filled with the next free number so the common case needs no typing. */
    suggestedNumber: number;
    onUploaded?: () => void;
}
export declare function EpisodeUploadForm({ showId, suggestedNumber, onUploaded, }: Props): React.JSX.Element;
export {};
//# sourceMappingURL=EpisodeUploadForm.d.ts.map