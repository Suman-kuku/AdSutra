{
  "name": "scriptcraft",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Promo generation platform — generate and refine ad promos from episode scripts.",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace @scriptcraft/shared",
    "dev:api": "npm run build:shared && npm run dev --workspace @scriptcraft/backend",
    "dev:web": "npm run build:shared && npm run dev --workspace @scriptcraft/frontend",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --build",
    "lint": "eslint .",
    "clean": "rm -rf shared/dist backend/dist frontend/dist frontend/dist-types */tsconfig.tsbuildinfo tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "eslint": "^9.14.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.13.0"
  },
  "engines": {
    "node": ">=20"
  }
}
